Place trained artifacts here when available:

- `lightgbm_model.pkl`
- `anomaly_model.pkl`

If `lightgbm_model.pkl` is absent, the API trains a LightGBM model from `DATA_PATH` on startup and writes the joblib bundle here.

